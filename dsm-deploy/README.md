# DRIVING SCHOOLS MANAGEMENT - Application Deployment

## Ansible

```bash
cd ansible

# to deploy our applications just run this command
ansible-playbook playbook.yml -u <ssh user on remote hosts>
```
